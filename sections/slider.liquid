<div class="home-slider">
  <div class="swiper homeSwiper">
    <div class="swiper-wrapper">
      {% for block in section.blocks %}
        <div class="swiper-slide">
          <a href="{{ block.settings.slide_link }}" class="slide-link">
            {% if block.settings.video %}
              {{ block.settings.video | video_tag: playsinline: true, autoplay: true, loop: true, muted: true, controls: false }}
            {% else %}
              <img class="desk-image" src="{{ block.settings.image | img_url: '2000x' }}" alt="" width="" height="" loading="lazy">
              <img class="mob-image" src="{{ block.settings.image_mob | img_url: '800x' }}" alt="" width="" height="" loading="lazy">
            {% endif %}
          </a>
          <div class="container slider-content">
            <span class="subtitle">{{ block.settings.subtitle }}</span>
            <h2 class="title">{{ block.settings.slide_title }}</h2>
            <a href="{{ block.settings.slide_link }}" class="button">
              {{ block.settings.cta_text | default: 'DISCOVER MORE' }}
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                <path d="M14.4301 5.92993L20.5001 11.9999L14.4301 18.0699" stroke="currentColor" stroke-width="1.5" stroke-miterlimit="10" stroke-linecap="round" stroke-linejoin="round"/>
                <path d="M3.5 12H20.33" stroke="currentColor" stroke-width="1.5" stroke-miterlimit="10" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
            </a>
          </div>
        </div>
      {% endfor %}
    </div>
    <div class="swiper-pagination"></div>
  </div>
</div>

<style>
.shopify-section-slider {
  position: relative;
  z-index: 1;
  transform: translate3d(0, 0, 0);
  -webkit-transform: translate3d(0, 0, 0);
  will-change: transform;
}

.home-slider {
  width: 100%;
  position: relative;
  z-index: 1;
  margin-bottom: 0;
  transform: translate3d(0, 0, 0);
  -webkit-transform: translate3d(0, 0, 0);
  will-change: transform;
}

#shopify-section-template--15771808956534__product-grid {
  position: relative;
  z-index: 2;
  transform: translate3d(0, 0, 0);
  -webkit-transform: translate3d(0, 0, 0);
  will-change: transform;
}

.home-slider .swiper-slide {
  position: relative;
  height: 650px;
}

.home-slider .swiper-slide img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.home-slider .slider-content {
  position: absolute;
  bottom: 180px;
  left: 0;
  right: 0;
  color: #fff;
  z-index: 2;
  max-width: 1400px;
  margin: 0 auto;
  padding: 0 4rem;
}

.home-slider .slider-content::before {
  content: '';
  position: absolute;
  top: -400px;
  left: -200vw;
  right: -200vw;
  bottom: -200px;
  background: linear-gradient(0deg, 
    rgba(0,0,0,0.7) 0%,
    rgba(0,0,0,0.5) 25%,
    rgba(0,0,0,0.3) 50%,
    rgba(0,0,0,0.1) 75%,
    rgba(0,0,0,0) 100%
  );
  z-index: -1;
}

.home-slider .slide-link {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: 1;
}

.home-slider .subtitle {
  display: block;
  font-size: 14px;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  margin-bottom: 1.25rem;
  font-weight: 500;
}

.home-slider .title {
  font-size: 48px;
  margin: 0 0 2.5rem;
  max-width: 500px;
  font-family: "Playfair Display", serif;
  line-height: 1.2;
}

.home-slider .button {
  display: inline-flex;
  align-items: center;
  gap: 12px;
  padding: 14px 28px;
  background: #fff;
  color: #000;
  text-decoration: none;
  font-size: 14px;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  transition: all 0.3s ease;
  font-weight: 500;
}

.home-slider .button:hover {
  opacity: 0.9;
  transform: translateY(-2px);
}

.home-slider .button svg {
  width: 16px;
  height: 16px;
  transition: transform 0.3s ease;
}

.home-slider .button:hover svg {
  transform: translateX(4px);
}

.swiper-pagination {
  bottom: 30px !important;
  display: flex;
  gap: 15px;
  justify-content: center;
  align-items: center;
  padding: 20px 0;
  width: 100%;
}

.swiper-pagination-bullet {
  width: 30px;
  height: 4px;
  background: rgba(255, 255, 255, 0.5);
  opacity: 1;
  transition: all 0.3s ease;
  margin: 0 !important;
  border-radius: 0;
  position: relative;
  cursor: pointer;
}

.swiper-pagination-bullet::after {
  content: '';
  position: absolute;
  top: -20px;
  left: -15px;
  right: -15px;
  bottom: -20px;
}

.swiper-pagination-bullet-active {
  background: #fff;
  transform: scaleX(1.2);
}

@media screen and (max-width: 767px) {
  .desk-image {
    display: none;
  }
  
  .home-slider {
    margin-bottom: 50px;
  }

  .home-slider .swiper-slide {
    height: 550px;
  }
  
  .home-slider .slider-content {
    bottom: 140px;
    padding: 0 1.5rem;
  }
  
  .home-slider .subtitle {
    font-size: 12px;
    margin-bottom: 1rem;
  }
  
  .home-slider .title {
    font-size: 32px;
    margin-bottom: 2rem;
    max-width: 100%;
    line-height: 1.15;
  }
  
  .home-slider .button {
    padding: 12px 24px;
    font-size: 12px;
    gap: 8px;
  }
  
  .home-slider .button svg {
    width: 14px;
    height: 14px;
  }

  .swiper-pagination {
    bottom: 25px !important;
    gap: 12px;
  }

  .swiper-pagination-bullet {
    width: 28px;
    height: 4px;
  }

  .swiper-pagination-bullet::after {
    top: -15px;
    left: -12px;
    right: -12px;
    bottom: -15px;
  }
}

@media screen and (max-width: 480px) {
  .home-slider {
    margin-bottom: 30px;
  }

  .home-slider .swiper-slide {
    height: 500px;
  }
  
  .home-slider .slider-content {
    bottom: 120px;
  }
  
  .home-slider .title {
    font-size: 28px;
    margin-bottom: 1.75rem;
  }
  
  .swiper-pagination {
    bottom: 20px !important;
  }
}

@media screen and (min-width: 768px) {
  .mob-image {
    display: none;
  }
}
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
  // Force layout recalculation
  document.body.style.opacity = '0.99';
  setTimeout(function() {
    document.body.style.opacity = '1';
  }, 0);

  // Initialize Swiper
  new Swiper('.homeSwiper', {
    effect: 'fade',
    fadeEffect: {
      crossFade: true
    },
    autoplay: {
      delay: 5000,
      disableOnInteraction: false
    },
    pagination: {
      el: '.swiper-pagination',
      clickable: true
    },
    loop: true,
    speed: 1000,
    on: {
      init: function() {
        // Force layout update after Swiper initialization
        window.dispatchEvent(new Event('resize'));
      }
    }
  });
});
</script>

{% schema %}
{
  "name": "Hero Slider",
  "tag": "section",
  "class": "slider",
  "settings": [],
  "blocks": [
    {
      "name": "Slide",
      "type": "slide",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Desktop Image"
        },
        {
          "type": "image_picker",
          "id": "image_mob",
          "label": "Mobile Image"
        },
        {
          "type": "video",
          "id": "video",
          "label": "Video"
        },
        {
          "type": "text",
          "id": "subtitle",
          "label": "Subtitle",
          "default": "HANDMADE EXCELLENCE"
        },
        {
          "type": "text",
          "id": "slide_title",
          "label": "Title",
          "default": "Artisan Home Decor"
        },
        {
          "type": "url",
          "id": "slide_link",
          "label": "Slide Link"
        },
        {
          "type": "text",
          "id": "cta_text",
          "label": "Button Text",
          "default": "DISCOVER MORE"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Hero Slider",
      "blocks": [
        {
          "type": "slide"
        }
      ]
    }
  ]
}
{% endschema %}