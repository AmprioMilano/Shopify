{% include 'breadcrumbs' %}
<div class="container">
  <div class="left-page-block">
    <div class="page-content">
      <h2>{{ page.title }}</h2>
      {{ page.content }}
      
    </div>
  </div>
</div>